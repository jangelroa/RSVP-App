<%= form_for(@event_news) do |f| %>
  <% if @event_news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_news.errors.count, "error") %> prohibited this event_news from being saved:</h2>

      <ul>
      <% @event_news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
